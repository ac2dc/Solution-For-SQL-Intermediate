create table table1 (ID INT IDENTITY, [Name] VARCHAR(200), Last4SSN INT, Salary MONEY,DateOfBirth DATETIME);

-- Create a stored procedure to generate random data to insert 10,000 rows into this table.

